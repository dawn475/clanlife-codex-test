# Legacy Vanilla Clanlife

This folder keeps the earlier HTML/CSS/JavaScript version of Clanlife for reference while the main app moves to the Base44 React codebase.

The active app entry point is now the root `index.html` plus `src/main.jsx`.
